import 'package:flutter/material.dart';

class StudentCourseMaterialPage extends StatelessWidget {
  const StudentCourseMaterialPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
