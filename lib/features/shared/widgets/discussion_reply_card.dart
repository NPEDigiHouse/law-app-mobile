import 'package:flutter/material.dart';

class DiscussionReplyCard extends StatelessWidget {
  const DiscussionReplyCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
