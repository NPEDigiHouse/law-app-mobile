import 'package:flutter/material.dart';

class TeacherDiscussionPage extends StatelessWidget {
  const TeacherDiscussionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Teacher Discussion Page'),
      ),
    );
  }
}
